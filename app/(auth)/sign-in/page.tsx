import FormLogin from "@/components/FormLogin";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Yayan Faturrohman - Sign In",
};

const PageSignIn = () => {
  return <FormLogin />;
};

export default PageSignIn;
